const config: {
  topMenu: boolean;
  rtl: boolean;
  darkMode: boolean;
  mainTemplate: string;
} = {
  topMenu: true,
  rtl: false,
  darkMode: false,
  mainTemplate: 'lightMode',
};

export default config;
